#ifndef COMMON_H
#define COMMON_H

namespace PaymentCore {
    // Константы Программиста А (исправили пропущенную точку с запятой)
    const int MAX_NAME_LEN = 50; // определение длины имени аккаунта

    // Константы Программиста Б (Твои константы для бинарного хранилища)
    const int DB_VERSION = 1;
    const int BLOCK_SIZE = 256;
}

#endif // COMMON_H
